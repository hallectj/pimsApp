<h2>Create a Nurse's Note:</h2>
<%= simple_form_for @patient.treatment.n_notes.build, url: new_n_note_path do |f| %>
    <%= f.input :name %>
    <%= f.input :message %>
    <%= f.submit :submit %>
<% end %>